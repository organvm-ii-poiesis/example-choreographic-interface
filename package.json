{
  "name": "example-choreographic-interface",
  "version": "0.1.0",
  "description": "Choreographic interface toolkit for mapping motion capture to generative outputs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
